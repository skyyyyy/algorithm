implement squrare without using * and caret.

int square(int x){
  if(x < 0) return square(-x);
  if(x == 0 || x == 1) return 1;
  else return square(x-1)+x+x-1;
}

what if double?

double square(double x){
   return x/1.0/x;
}
